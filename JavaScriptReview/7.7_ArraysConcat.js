const daughters = ['Soraia','Maria'];
const children = ['Pedro','Henrique'];
var all = daughters.concat(children); //concate 2 arrays
console.log(all,children,daughters);
var all = daughters.concat(children,'Pita'); //concat 2 arrays and add another element 
console.log(all,children,daughters);